{
  "description": "Extracts structured fields from scanned/submitted customer complaint forms.",
  "tags": {
    "family": "complaint"
  },
  "baseAnalyzerId": "prebuilt-document",
  "config": {
    "returnDetails": true
  },
  "models": {
    "completion": "gpt-5"
  },
  "fieldSchema": {
    "name": "ComplaintFormSchema",
    "description": "Fields captured on a customer complaint form.",
    "fields": {
      "CustomerFirstName": { "type": "string", "description": "Customer's first name." },
      "CustomerLastName": { "type": "string", "description": "Customer's last name." },
      "EmailAddress": { "type": "string", "description": "Customer's email address." },
      "PhoneNumber": { "type": "string", "description": "Customer's phone number." },
      "NatureOfComplaint": { "type": "string", "description": "Short category/summary of the complaint, e.g. 'Late delivery', 'Product defect'." },
      "IncidentDetails": { "type": "string", "description": "Free-text description of what happened." },
      "DesiredResolution": { "type": "string", "description": "What the customer wants done to resolve the complaint." }
    }
  }
}
