{
  "note": "Maps environment names to Content Understanding endpoints and optional labeled-data storage. Replace the placeholder values before using scripts with -Environment. -Endpoint always overrides this file.",
  "environments": {
    "dev": {
      "endpoint": "https://<your-dev-resource>.cognitiveservices.azure.com",
      "labeledDataContainerUrl": "https://<your-dev-storage>.blob.core.windows.net/<container>"
    }
  }
}
